## Jotnar server only — for use with a local inference backend (e.g. Ollama).
##
## Usage:
##   docker compose -f docker-compose.local.yml up
##
## Set INFERENCE_HOST to wherever your OpenAI-compatible backend is running.
## For Ollama on the host machine:
##   INFERENCE_HOST=http://host.docker.internal:11434 docker compose -f docker-compose.local.yml up

services:
  jotnar:
    image: ghcr.io/UmushiUmushi/jotnar-server:latest
    ports:
      - "8910:8910"
    volumes:
      - jotnar-data:/data
    environment:
      - INFERENCE_HOST=${INFERENCE_HOST:-http://host.docker.internal:11434}
      - INFERENCE_TIMEOUT_SEC=120
      - INFERENCE_MAX_RETRIES=3
      - JOTNAR_DB_PATH=/data/journal.db
    restart: unless-stopped

volumes:
  jotnar-data:
